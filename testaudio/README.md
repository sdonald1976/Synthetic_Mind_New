# Test audio

Sample clips for the audio findings (014, 015) and the `SyntheticMind.Listen` demos.

| File | Source | Notes |
|---|---|---|
| `jfk.wav` | whisper.cpp `samples/jfk.wav` ([MIT](https://github.com/ggerganov/whisper.cpp)) | JFK inaugural address ("ask not…"). The speech itself is a US-government work / public domain. 16 kHz mono. |
| `music.wav` | pydub `test/data/test1.wav` ([MIT](https://github.com/jiaaro/pydub)) | A short music clip used as a test fixture. Original recording's copyright is unverified — fine as a local research fixture, but swap for something clearly licensed before any public/commercial use. |

Both are committed only so the experiments run without a download. Regenerate at any time from the URLs above.
